Description:

This code searches for repositories which will be used to test Etch.

Use Python 3.11.5

Ensure that all csv files are already set up before running the code: favorites.csv and advanced_favorites.csv

Ensure that all folders are already set up before running the code: favorites_repos and temp_cloned_repos

To run the code use "streamlit run app.py" enter this in the console.






